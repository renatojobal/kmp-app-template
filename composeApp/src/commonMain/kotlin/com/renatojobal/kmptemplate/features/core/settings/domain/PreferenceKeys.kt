package com.renatojobal.kmptemplate.features.core.settings.domain

/**
 * Central place for shared preference / NSUserDefaults / settings keys.
 *
 * Add string constants here as features grow — keeps the key strings out of
 * usage sites and lets a quick `grep` find every consumer.
 */
object PreferenceKeys {
    // Example — replace or extend as you wire features.
    const val EXAMPLE_KEY = "example_key"
}
